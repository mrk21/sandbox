<%= form_with(model: user3) do |form| %>
  <% if user3.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user3.errors.count, "error") %> prohibited this user3 from being saved:</h2>

      <ul>
        <% user3.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
