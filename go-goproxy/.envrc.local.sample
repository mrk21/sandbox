#-------------------------------------------------------------------------------
# Docker
#-------------------------------------------------------------------------------
export DOCKER_HOST_PROXY_PORT=8080 # You can access to proxy by http://127.0.0.1:${DOCKER_HOST_PROXY_PORT}
export DOCKER_HOST_WEB_PORT=80     # You can access to nginx by http://127.0.0.1:${DOCKER_HOST_WEB_PORT}
