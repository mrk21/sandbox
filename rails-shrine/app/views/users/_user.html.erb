<div id="<%= dom_id user %>">
  <p>
    <strong>Name:</strong>
    <%= user.name %>
  </p>

  <p>
    <strong>Avatar:</strong>
    <%= link_to image_tag(user.avatar_url(:small)), user.avatar_url if user.avatar.present? %>
  </p>
</div>
